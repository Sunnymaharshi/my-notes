{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run content && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
